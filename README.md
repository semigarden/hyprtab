# hyprtab

Hyprland plugin for formatting window titles

## Config

Path:

- `~/.config/hypr/plugins/hyprtab.conf`

Key:

- `format = "..."`

Variables:

- `{default}`
- `{tab}`
- `{app}`
- `{class}`
- `{initialTitle}`
- `{initialClass}`
